extends layout

block content
  h1= title
  p Welcome to #{title}

  a(href="/new") Add a new message

  //- - var messageArray = messages
  each message in messages
      p #{message.user} - #{message.text} (#{message.added})



  
